languaje: android
android:
  components:
    - build-tools-23.0.1
    - android-23
    - extra
  cache:
    directories:
      - node_modules
  before-install:
    - node --version
  install:
    npm install
  script:
    - npm test
  branches:
    only:
      - master
